COMP = boot/arch/x86

CXX_FILES = 
S_FILES = boot.S

DIR = ${TOPDIR}/${COMP}
COMP_SRC += ${S_FILES:%=${DIR}/%} ${CXX_FILES:%=${DIR}/%}

#${DIR}/boot.o: ${DIR}/boot.S
#	@echo -e "\tAssembling" $@ "from " $^
#	$(AS) -o $@ $<
